// (Intentionally left minimal.) Previous chrome namespace augmentations for
// lastError, sendMessage overload, and OnUpdatedInfo have been removed after
// refactoring code to align with current chrome-types definitions.
// File retained to allow future targeted augmentations if strictly required.
