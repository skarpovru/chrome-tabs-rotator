{
  // Background Jasmine spec build (CommonJS) compiling only test + source files needed for Node-based execution.
  // Uses *.bg-spec.ts naming to avoid accidental inclusion in production build.
  "extends": "./tsconfig.background.json",
  "compilerOptions": {
    "outDir": "./out-tsc/background-spec",
    "module": "CommonJS",
    // Override base/moduleResolution for CommonJS runtime so extensionless relative imports work.
    "moduleResolution": "node",
    "types": ["chrome-types", "jasmine", "node"],
    "target": "ES2022"
  },
  "include": [
    "src/background/**/*.ts",
    "src/background/__tests__/**/*.bg-spec.ts",
    "src/background/__tests__/test-helpers/**/*.ts",
    "src/types/**/*.d.ts"
  ]
}
