{
  // Tooling / configuration TS (noEmit). Includes playwright config + build scripts.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["node"]
  },
  "include": [
    "playwright.config.ts",
    "scripts/**/*.ts"
  ]
}
