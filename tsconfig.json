{
  // Root solution-style tsconfig. Added minimal compilerOptions below only to satisfy
  // editor/lint recommendations (Edge Tools) about enabling strict mode and consistent casing.
  // Actual compilation settings live in the referenced project configs.
  "compilerOptions": {
    "strict": true,
    "forceConsistentCasingInFileNames": true
  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.background.json" },
    { "path": "./tsconfig.harness.json" }
  ]
}
